{
  "name": "atpls-share",
  "private": true,
  "scripts": {
    "setup": "echo 'run ./bin/setup inside docker:bash'",
    "start": "npm run docker:server",
    "docker:start": "cd .dockerdev && npm run start",
    "docker:stop": "cd .dockerdev && npm run stop",
    "docker:server": "cd .dockerdev && npm run server",
    "docker:bash": "cd .dockerdev && npm run bash",
    "server": "bundle exec rails server"
  },
  "dependencies": {
    "@rails/webpacker": "^4.2.0"
  },
  "devDependencies": {
    "form-data": "^2.5.1",
    "node-fetch": "^2.6.1",
    "webpack-dev-server": "^3.9.0"
  }
}
